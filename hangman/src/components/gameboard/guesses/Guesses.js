import './Guesses.css';

const guesses = ['x', 'b', 'k', 'c'];
const wrongGuesses = ['x', 'b', 'k'];

const Guesses = () => {
	return (
    <div className="Guesses">
    </div>
	);
};

export default Guesses;